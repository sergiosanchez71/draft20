{
    "name": "draft20",
    "private": true,
    "version": "1.0.0",
    "description": "Draft 20 — build de CSS (Tailwind) y minificado de JS",
    "scripts": {
        "build:css": "tailwindcss -i css/tailwind.src.css -o css/tailwind.css --minify",
        "build:js": "terser js/app.core.js -c -m -o js/app.core.min.js && terser js/app.game.js -c -m -o js/app.game.min.js && terser js/bot_policy.js -c -m -o js/bot_policy.min.js",
        "build": "npm run build:css && npm run build:js"
    },
    "devDependencies": {
        "tailwindcss": "3.4.17",
        "terser": "5.36.0"
    }
}
