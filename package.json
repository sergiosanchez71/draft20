{
    "name": "draft20",
    "private": true,
    "version": "1.1.15",
    "description": "Draft 20 — build de CSS (Tailwind) y minificado de JS",
    "scripts": {
        "build:css": "tailwindcss -i css/tailwind.src.css -o css/tailwind.css --minify",
        "build:js": "terser js/app.core.js -c -m -o js/app.core.min.js && terser js/app.game.js -c -m -o js/app.game.min.js && terser js/bot_policy.js -c -m -o js/bot_policy.min.js",
        "build": "npm run build:css && npm run build:js",
        "test": "npm run test:php && npm run test:node",
        "test:php": "php tools/test_catalogo.php && php tests/rate_limit.test.php",
        "test:node": "node tests/bot_policy.test.js && node tests/a11y.test.js",
        "smoke": "php tools/smoke.php",
        "gen:assets": "node tools/gen_emoji.js && node tools/gen_flags.js && svgo --folder img/emoji --multipass"
    },
    "devDependencies": {
        "axe-core": "^4.13.0",
        "jsdom": "^22.1.0",
        "svgo": "^4.1.0",
        "tailwindcss": "3.4.17",
        "terser": "5.36.0"
    }
}
